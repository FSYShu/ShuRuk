<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="ShuRuk.App.Pages.ModulePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top bar: SelectorBar + AutoSuggestBox. No padding, flush to edges. -->
        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="320" />
            </Grid.ColumnDefinitions>

            <SelectorBar
                x:Name="SubBar" AutomationProperties.Name="Module tab bar"
                Grid.Column="0"
                Margin="8,0,8,0"
                Height="48"
                SelectionChanged="SubBar_SelectionChanged">
                <SelectorBarItem
                    x:Name="ModulePageInstalledTab"
                    x:Uid="ModulePageInstalledTab"
                    IsSelected="True">
                    <SelectorBarItem.Icon>
                        <FontIcon Glyph="&#xE896;" />
                    </SelectorBarItem.Icon>
                </SelectorBarItem>
                <SelectorBarItem
                    x:Name="ModulePageDiscoverTab"
                    x:Uid="ModulePageDiscoverTab">
                    <SelectorBarItem.Icon>
                        <FontIcon Glyph="&#xE721;" />
                    </SelectorBarItem.Icon>
                </SelectorBarItem>
            </SelectorBar>

            <AutoSuggestBox
                x:Name="SearchBox"
                Grid.Column="1"
                Margin="8,0,8,0"
                VerticalAlignment="Center"
                MinHeight="32"
                MaxHeight="32"
                x:Uid="ModulePageSearchBox"
                QueryIcon="Find"
                TextChanged="SearchBox_TextChanged" />
        </Grid>

        <!-- Content area with 16px padding. -->
        <Grid Grid.Row="1" Padding="16">
            <Frame
                x:Name="ContentFrame"
                IsNavigationStackEnabled="False" />
        </Grid>
    </Grid>

    <VisualStateManager.VisualStateGroups>
        <VisualStateGroup>
            <VisualState x:Name="WideState">
                <VisualState.StateTriggers>
                    <AdaptiveTrigger MinWindowWidth="1024" />
                </VisualState.StateTriggers>
            </VisualState>
            <VisualState x:Name="NarrowState">
                <VisualState.StateTriggers>
                    <AdaptiveTrigger MinWindowWidth="0" />
                </VisualState.StateTriggers>
                <VisualState.Setters>
                    <Setter Target="SearchBox.(Visibility)" Value="Collapsed" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>
</Page>
